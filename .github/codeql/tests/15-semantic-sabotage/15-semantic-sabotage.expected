| mock-rule-15-semantic-integrity-sabotage-gaslighting.ts:5:15:5:77 | joplin. ... ged' }) | Semantic Sabotage: The plugin is mutating or deleting notes directly inside a workspace event hook (e.g., `onNoteSelectionChange`). \\n**Reviewer Action:** Modifying a note the exact moment a user views or edits it can mimic "gaslighting" malware. Ensure these modifications are expected, visible formatting changes (like an auto-linter), not destructive silent edits. |
| mock-rule-15-semantic-integrity-sabotage-gaslighting.ts:9:15:9:53 | joplin. ... nt.id]) | Semantic Sabotage: The plugin is mutating or deleting notes directly inside a workspace event hook (e.g., `onNoteSelectionChange`). \\n**Reviewer Action:** Modifying a note the exact moment a user views or edits it can mimic "gaslighting" malware. Ensure these modifications are expected, visible formatting changes (like an auto-linter), not destructive silent edits. |
| mock-rule-15-semantic-integrity-sabotage-gaslighting.ts:13:15:13:68 | joplin. ... dated') | Semantic Sabotage: The plugin is mutating or deleting notes directly inside a workspace event hook (e.g., `onNoteSelectionChange`). \\n**Reviewer Action:** Modifying a note the exact moment a user views or edits it can mimic "gaslighting" malware. Ensure these modifications are expected, visible formatting changes (like an auto-linter), not destructive silent edits. |
